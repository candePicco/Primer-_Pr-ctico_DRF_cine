[project]
name = "cine-api"
version = "0.1.0"
description = "API REST en Django para administrar un cine"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2,<6.0",
    "djangorestframework>=3.15,<4.0",
]
